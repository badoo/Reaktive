setupMultiplatformLibrary(project)
setupAllTargetsWithDefaultSourceSets(project: project, isLinuxArm32HfpEnabled: false)

kotlin {
    targets {
//        fromPreset(presets.jvm, 'jvmJsCommon')
//        fromPreset(presets.linuxX64, 'nativeCommon')
    }

    sourceSets {
        commonMain {
            dependencies {
                implementation deps.kotlinxCoroutinesCoreCommon
                implementation project(':reaktive')
                implementation project(':utils')
            }
        }

        commonTest {
            dependencies {
                implementation project(':reaktive-testing')
            }
        }

        jvmMain {
            dependencies {
                implementation deps.kotlinxCoroutinesCore
            }
        }

        androidMain {
            dependencies {
                implementation deps.kotlinxCoroutinesAndroid
            }
        }

        nativeCommonMain {
            dependencies {
                implementation deps.kotlinxCoroutinesCoreNative
            }
        }

        jsMain {
            dependencies {
                implementation deps.kotlinxCoroutinesCoreJs
            }
        }
    }
}

apply plugin: PublishPlugin