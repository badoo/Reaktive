buildscript {
    ext.reaktive_version = '1.1.16'
    ext.reaktive_group_id = 'com.badoo.reaktive'
}

plugins {
    id 'binary-compatibility-configuration'
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
